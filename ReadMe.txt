The project have the dataset hardcoded in them.
Working directory has to be set to the project folder location before starting to run the code.

For example:
> setwd("C:/Phase3/Project")

The project generates output for 4 different models and saves them as a .csv file in the project folder location.
The confusion matrix graph is generated in RStudio and can be viewed in the "Plots" tab.